{
  "$schema": "http://json-schema.org/schema",
  "$id": "https://example.com/product.schema.json",
  "title": "Product",
  "description": "Configuration for a BR project in VS Code",
  "type": "object",
  "properties": {
    "layoutFolder": {
      "description": "The folder where Fileio layouts are stored.",
      "type": "string"
    },
    "searchFolder": {
      "description": "A subfolder to add to the base path when searching libraries. Defaults to '.' or project file location.",
      "type": "string"
    },
    "globalIncludes": {
      "description": "List of files to look for additional completions.",
      "type": "array",
      "items": {
        "description": "Library Files",
        "type": "string"
      }
    }
  }
}